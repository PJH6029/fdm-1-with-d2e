Query type: result-phase-exit-review

I am reproducing the public FDM-1 training recipe on real D2E data. Phase 0 is only the data/evaluator/sanity foundation, not model training: D2E/OWAMcap reader for labeled `.mkv`+`.mcap`, 60fps→50ms sampling, 50ms action binning, tokenizer/de-tokenizer, prediction-to-event writer scaffolding, train/val/test and scale manifests, per-game action distributions, evaluator-compatible round-trips, official D2E reference path, and floor/probe scaffolds for later phases.

My current hypothesis is that Phase 0 is evidence-complete enough to exit: the implementation is committed/pushed, local tests pass, real D2E discovery and action distributions run on MLXP, and the pinned official D2E evaluator can score a same-MCAP ground-truth round-trip after a documented runtime-library recovery. Remaining gaps seem appropriate follow-ups for later phases, not Phase 0 blockers.

Decisive local evidence:
1. Local code now has a uv-managed package, D2E discovery, optional JSON/JSONSchema MCAP reader/decoder, 50ms binning, action tokenizer/de-tokenizer, manifest/split/scale builders, local D2E-style metrics, official D2E reference records, and floor/probe scaffolds. Local verification: `uv run --locked pytest` = 57 passed; compileall and `git diff --check` passed.
2. Cluster final run used pushed SHA `f801bce287bafbd8ea8b1d2e225dcd707a8fba72` and image `docker.io/pjh6029/fdm-1-with-d2e:phase0-20260605-f801bce@sha256:4a78401935fa9660aab5f17ea5872dfc21a1f67cba0df269a4ac50ae287d2ae1`. The MLXP production pod was cancelled after artifact capture; dataset root `/mnt/ddn/extra-ddn-continuous-gui/` was treated read-only and outputs were written under the repo clone.
3. Real D2E manifest discovery found 459 labeled `.mkv`/`.mcap` pairs across 29 games. Deterministic split counts were train 367, validation 46, test 46; scale counts were 5%=36, 10%=50, 50%=193, 100%=367.
4. Real committed CLI action distribution ran one recording per game through `--from-mcap-json`: 29 recordings/29 games, 1,832,113 50ms bins, 1,161,652 events, no-op rate 0.516233987751, overflow rate 0.0. It uses `mouse/raw last_x,last_y` for relative motion; absolute mouse move is diagnostic-only; keyboard labels are stable `VK_<int>` pending later policy audit.
5. Evaluator sanity: local tokenization/de-tokenization/writer-record round-trip had all six local metric values = 1.0. Pinned official `worv-ai/D2E` commit `80e98e26e4dc584ec76fec5789b4a97c275dd032` `evaluate.py` scored the same Apex ground-truth MCAP as prediction: 29,242 bins, duration 1462.052619s, mouse Pearson X 1.0, mouse Pearson Y 0.9999999999999998, scale X/Y 1.0, mouse-button accuracy 1.0, keyboard key accuracy 1.0. This official run required pod-local installation of Debian runtime GUI libs (`libxcb1 libx11-6 libgl1 libglib2.0-0`) after the slim image initially failed to decode `desktop/ScreenCaptured`; I plan to bake those libs into a later official-evaluation image.

One focused question: under these constraints, is Phase 0 evidence strong enough to mark Phase 0 complete and proceed to Phase 0.5, or is there a minimal additional data/evaluator check that should block exit? Please critique hidden assumptions, possible confounds, and the cheapest additional evidence if you think exit is not yet justified.
